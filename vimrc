"backspace goes everywhere
set backspace=indent,eol,start

"setting tabs to 4 spaces
set tabstop=4
set shiftwidth=4
set expandtab

set history=10

if has('mouse')
  set mouse=a
endif

set backupdir=~/.vim/backup
syntax on
